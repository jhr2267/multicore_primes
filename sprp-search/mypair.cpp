#include "mypair.h"

MyPair::MyPair(unsigned a, unsigned b, unsigned first_composite, unsigned first_prime_pos) {
    this->a = a;
    this->b = b;
    this->first_composite = first_composite;
    this->first_prime_pos = first_prime_pos;
}